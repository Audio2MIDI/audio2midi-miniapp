# Деплой MiniApp визуализатора на audio2midi.ru

## Что это
Наш кастомный Piano Roll визуализатор для MIDI файлов. Работает целиком в браузере — серверная часть не нужна. Открывается как MiniApp внутри Telegram.

## Репозиторий
```
https://github.com/Audio2MIDI/audio2midi-miniapp.git
```

## Инструкция для Миши (5 минут)

### Вариант 1: Из собранных файлов (проще всего)

Билд уже собран в `frontend/dist/`. Это обычная статика — HTML + JS + CSS, 484 КБ.

```bash
# 1. Клонировать репо
git clone https://github.com/Audio2MIDI/audio2midi-miniapp.git
cd audio2midi-miniapp

# 2. Скопировать собранные файлы в нужную папку на сервере
# (пример — подставь свой путь, где лежит audio2midi.ru)
cp -r frontend/dist/* /путь/к/audio2midi.ru/app/

# 3. Готово! Визуализатор доступен по:
# https://audio2midi.ru/app/?file=https://s3.rapid-vision.ru/a2m/example.mid
```

### Вариант 2: Собрать с нуля

```bash
git clone https://github.com/Audio2MIDI/audio2midi-miniapp.git
cd audio2midi-miniapp/frontend

# Установить зависимости
npm install

# Собрать
npm run build

# Результат в dist/ — скопировать на сервер
cp -r dist/* /путь/к/audio2midi.ru/app/
```

### Настройка nginx (если нужно)

Добавить в конфиг nginx для audio2midi.ru:

```nginx
location /app/ {
    alias /путь/к/статике/app/;
    try_files $uri $uri/ /app/index.html;
}
```

Перезапустить nginx: `sudo nginx -s reload`

## Как это работает

1. Бот генерирует MIDI → загружает на S3
2. Бот отправляет кнопку "🎹 Piano Roll" с URL: `https://audio2midi.ru/app/?file=<s3_url>`
3. Telegram открывает этот URL как MiniApp (встроенный браузер)
4. Визуализатор загружает MIDI по URL и рисует piano roll

Серверная часть (бэкенд) не нужна — MIDI парсится прямо в браузере через JavaScript.

## После деплоя

Когда визуализатор будет доступен по `https://audio2midi.ru/app/`, нужно обновить URL в боте:
- Файл: `/home/jatana/Audio2MIDI/utils.py`
- Заменить `audio2midi.ru/visualizer` → `audio2midi.ru/app/`
- Изменение подхватится автоматически (hot-reload)

## Структура

```
audio2midi-miniapp/
├── frontend/           # React + TypeScript + Vite
│   ├── src/
│   │   ├── components/
│   │   │   └── PianoRoll.tsx    # Основной визуализатор
│   │   ├── api/                 # API клиент (для будущей истории)
│   │   ├── hooks/
│   │   │   └── useTelegram.ts   # Telegram WebApp SDK
│   │   └── App.tsx
│   ├── dist/           # ← Готовый билд (484 КБ)
│   └── package.json
├── backend/            # FastAPI (не нужен для визуализатора)
├── docker-compose.yaml
└── nginx.conf
```
