# Деплой MiniApp Piano Roll

## Что это
Кастомный Piano Roll визуализатор для MIDI файлов. Работает целиком в браузере — серверная часть не нужна. Используется как Telegram MiniApp (открывается внутри Telegram по кнопке).

## Репозиторий
```
https://github.com/Audio2MIDI/audio2midi-miniapp.git
```

## Требования
- Любой веб-сервер с HTTPS (nginx, caddy, apache, etc.)
- Node.js 18+ (только для сборки, не для работы)

## Деплой

### Вариант 1: Из готового билда

Билд уже собран в `frontend/dist/` — обычная статика (HTML + JS + CSS), 484 КБ.

```bash
git clone https://github.com/Audio2MIDI/audio2midi-miniapp.git
cd audio2midi-miniapp

# Скопировать статику на веб-сервер
cp -r frontend/dist/* /путь/к/сайту/app/
```

### Вариант 2: Собрать с нуля

```bash
git clone https://github.com/Audio2MIDI/audio2midi-miniapp.git
cd audio2midi-miniapp/frontend

npm install
npm run build

# Результат в dist/
cp -r dist/* /путь/к/сайту/app/
```

### Настройка nginx

```nginx
location /app/ {
    alias /путь/к/статике/app/;
    try_files $uri $uri/ /app/index.html;
}
```

```bash
sudo nginx -s reload
```

## Использование

Визуализатор принимает URL MIDI-файла через query-параметр `file`:

```
https://your-domain.com/app/?file=https://s3.rapid-vision.ru/a2m/example.mid
```

MIDI загружается и парсится прямо в браузере через JavaScript. Серверный бэкенд не нужен.

## Интеграция с ботом Audio2MIDI

Бот формирует кнопку "🎹 Piano Roll" с URL визуализатора. Настройка URL — в файле:
- `/home/jatana/Audio2MIDI/utils.py` — функции `upload_and_log()` и `send_files()`
- Переменная: `visualizer_url = f"https://audio2midi.ru/visualizer?file=..."`
- При смене домена/пути — поменять URL здесь

## Текущий статус
- **Наш визуализатор:** собран, не захостен (нужен сервер с HTTPS)
- **Временный:** используется `audio2midi.ru/visualizer` (старый визуализатор на сервере аudio2midi.ru)

## Структура проекта

```
audio2midi-miniapp/
├── frontend/                # React + TypeScript + Vite
│   ├── src/
│   │   ├── components/
│   │   │   └── PianoRoll.tsx    # Основной визуализатор (вертикальный + горизонтальный)
│   │   ├── api/                 # API клиент
│   │   ├── hooks/
│   │   │   └── useTelegram.ts   # Telegram WebApp SDK
│   │   └── App.tsx
│   ├── dist/                    # Готовый билд (484 КБ)
│   └── package.json
├── backend/                 # FastAPI (опционально, для будущей истории MIDI)
├── docker-compose.yaml      # Docker деплой (фронт + бэкенд + nginx)
├── nginx.conf
├── DEPLOY.md                # ← Этот файл
└── README.md
```
